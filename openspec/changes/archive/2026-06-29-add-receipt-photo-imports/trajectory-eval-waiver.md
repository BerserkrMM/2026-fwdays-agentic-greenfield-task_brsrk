# Trajectory eval waiver — add-receipt-photo-imports

No runnable LLM trajectory-eval workflow is available in this repository. Deterministic trajectory checks remain required and were run with `node scripts/check-trajectory.mjs`; maker≠checker review raw outputs are stored under `reviews/`.

This waiver does not claim an end-to-end trajectory-eval pass.

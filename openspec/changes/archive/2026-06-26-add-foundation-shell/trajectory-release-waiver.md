# Trajectory release waiver — add-foundation-shell

This slice predates the stricter Project Factory trajectory evidence discipline that was introduced later in the project. The archived change has `design.md` and `tasks.md`, but it does not have durable maker≠checker `review-findings.json`, and the original historical commit does not carry a `Slice: add-foundation-shell` trailer.

For release-mode trajectory checks, this waiver documents that the missing review evidence and trailer are a historical retrofit gap, not a current product/runtime defect. New slices must not rely on this waiver pattern; they must provide raw review evidence and commit trailers normally.

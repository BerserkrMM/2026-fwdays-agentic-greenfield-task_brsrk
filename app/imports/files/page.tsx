import { PlaceholderScreen } from "@/src/modules/foundation/ui/PlaceholderScreen";

export default function ImportFilesPage() {
  return (
    <PlaceholderScreen
      title="Фото чека"
      description="Одне фото чека → AI-розпізнавання → операції. PDF не підтримується."
    />
  );
}
